# Core dependencies
asyncio
aiohttp>=3.8.0
tenacity>=8.0.0
requests>=2.28.0
elasticsearch>=8.0.0
pyjwt>=2.0.0
email-validator>=2.0.0
# Data processing and utilities
numpy>=1.21.0
pandas>=1.3.0

# Logging and configuration
python-dotenv>=0.19.0

# Testing
pytest>=6.0.0
pytest-asyncio>=0.18.0
pytest-cov>=3.0.0


# Future API dependencies (for when we add FastAPI)
fastapi>=0.68.0
uvicorn>=0.15.0
pydantic>=1.8.0

# Vector database clients (for future integration)
# qdrant-client>=1.0.0
pinecone>=2.0.0

# Elasticsearch client (for future integration)
elasticsearch>=8.0.0

# SPARQL client (for future integration)
sparql-client>=0.1.0

# LLM clients (for future integration)
openai>=1.0.0
anthropic>=0.3.0

# Caching and message queuing (for future integration)
redis>=4.0.0
aioredis>=2.0.0

# Monitoring and metrics (for future integration)
prometheus-client>=0.12.0

# Development tools
black>=22.0.0
flake8>=4.0.0
mypy>=0.950
